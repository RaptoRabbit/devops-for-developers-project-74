services:
  js-fastify-blog:
    image: node:20.12.2
    working_dir: /app
    volumes:
      - ./app:/app
    ports:
      - "8080:8080"
    command: bash -lc "make setup && make dev"
    user: "${UID:-1000}:${GID:-1000}"
    environment:
      - NODE_ENV=development
